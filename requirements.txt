# Face Mask Detector — Python Dependencies
# Install with: pip install -r requirements.txt
#
# Python >= 3.9 recommended
# CPU-only training supported (no GPU required)

# Deep learning
tensorflow>=2.13.0          # Includes Keras — use tensorflow-cpu if you want a smaller install

# Dataset download from Kaggle
kagglehub>=0.3.0

# Data processing
numpy>=1.24.0
scikit-learn>=1.3.0         # For class weights, confusion matrix, classification report

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# ─── Optional (CPU performance) ───────────────────────────────────────────────
# Uncomment if you want a CPU-only TensorFlow (smaller download, no GPU overhead)
# tensorflow-cpu>=2.13.0    # Replace tensorflow above with this line
